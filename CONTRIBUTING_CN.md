# 参与共建

[English](CONTRIBUTING.md) · **中文**

感谢考虑参与。这是一个由"手上有这块硬件的人"维护的单板项目，所以最有价值的贡献，是那些**能在真机 Tab5 上验证**的。

## 可以帮什么

- **从 [ROADMAP.md](ROADMAP.md) 里挑一项**，先在 issue 下留言（或开一个）说明你在做，避免撞车。
- **修 bug** —— 请带上下面说的证据。
- **改进文档** —— Windows 构建走查、实机截图、翻译。
- **把本版本没用的硬件用起来** —— ES8388 音频、BMI270 IMU、INA226 电量计、RX8130CE 中断线，硬件都在，这里一个都没接。

## 项目约定（这些来自真实故障，不是口味）

1. **一次刷机只改一个看得见的东西。** 刷进去、看屏幕、再继续。一次刷两个改动，出回归时无法归因。
2. **用眼睛验证，不要靠串口调试循环。** 在这颗 SoC 上，USB-CDC 流量会和显示通路抢 PSRAM 带宽，甚至可能就是把它弄挂的那一手（见 [BUILDING_CN.md §4](BUILDING_CN.md)）。抓下开机横幅，然后拔线，用眼睛看屏幕。
3. **热路径保持单写者、够便宜。** 任何引入第二个帧缓冲写入者、在 `loop()` 里用全屏 sprite、或每帧大块分配内存的改动，都必须放在**运行时开关**后面（NVS 标志或一个文件），默认保持当前行为——坏实验必须能**不重刷**就回退。在这块板子上，绘制通路过于激进不只是掉帧，它可能把 DSI 锁死到帧保护把板子重启为止。
4. **不要删掉帧超时保护，也不要删看门狗喂狗**，除非你有更好的替代方案。见 [BUILDING_CN.md §7](BUILDING_CN.md)。
5. **从已知可用的基线出发；宁可小步改动，不要重写。** 一个能验证的 10% 提升，胜过可能是 40% 的重写。

## 构建与测试

见 [BUILDING_CN.md](BUILDING_CN.md)。开 PR 之前：

- [ ] 在**干净树**里能编过（`rm -rf .pio && pio run`），不只是增量。
- [ ] 设备能启动、时钟能渲染、`FPS:` 稳定在 30 左右。
- [ ] 雨不留残影：至少观察一个完整的列复位周期（约一分钟），确认清扫过的竖条被清干净了。
- [ ] 进出设置菜单的四个子界面后，都能回到干净的时钟画面。
- [ ] 如果动了显示通路：插着 USB 和拔掉 USB 各跑 10 分钟以上，不冻屏。
- [ ] 如果是有风险的改动：已在运行时开关后面，并且你写清了开关怎么用。

## 报 bug

用 issue 表单，并带上：

- **你看到了什么** —— 屏幕行为，以及开机横幅那一行。
- **一条 `FPS:` 样本** —— 它还在打印吗？还是在冻屏之前串口就没声了？
- **是怎么恢复的** —— 自己重启了，还是你必须断电？这一个细节就能区分软件 panic 和硬件级挂死，两者根因完全不同。
- **当时是否插着 USB** —— 这是已知的加重因素，不是无关信息。

## 提交与 PR 风格

- 推荐用约定式前缀（`feat:`、`fix:`、`docs:`、`perf:`、`refactor:`、`chore:`）。
- **中文或英文都可以**，用哪个能把改动说清楚就用哪个。
- 正文里解释**为什么**，尤其是涉及绘制通路、时序、PSRAM 流量的改动。
- 改了版本号时，`src/main.cpp`（开机横幅）、About 界面和 [CHANGELOG.md](CHANGELOG.md) 三处保持同步。

## 代码风格

跟随周围的代码：2 空格缩进、全局变量 `g_` 前缀、常量用 `SCREAMING_CASE` 放在 `config.h`、文件内私有的一律 `static`。绿底黑字界面用 `src/matrix_gui.h` 里的 `MG::` 调色板——**不要引入新颜色**，这套色板是刻意保持单色绿的。

任何绕过板级限制的写法，加一句注释说明原因，例如「Tab5: hosted 链路的管脚与库默认值不同」。下一个人需要知道哪些行是可移植的、哪些是这块板子专属的。

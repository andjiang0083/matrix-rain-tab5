# 路线图

[English](ROADMAP.md) · **中文**

大致按价值排序。每项都拆到了"手上有 Tab5 的新人就能上手"的粒度。**动手前先在 issue 下留言（或开一个）**，避免撞车。

## 1. 显示通路 —— 用更少的字节做同样的事

30fps 是**刻意设的天花板**，不是量出来的极限：我们压住它，是因为每秒画得越多，CPU 往 PSRAM 的回写就越多，而 DSI 同时在 DMA 读同一片 PSRAM——这正是过去会把面板弄挂的那个区间（见 [docs/PORTING-NOTES.md](docs/PORTING-NOTES.md)）。所以这里真正有意思的方向是让**每帧更便宜**，而不是更频繁：

- [ ] **跳过头部还没走满一格的列。** 现在每帧每列都重画整条拖尾。请给出改动前后的实测绘制帧率，以及 10 分钟的冻屏检查。
- [ ] **对最暗的拖尾格重测 `drawChar` vs `fillRect`**（最初的实测：`fillRect` 便宜约 2–3 倍）。若确实更好，放在运行时开关后面，默认关。
- [ ] **把 DSI 卡死的机理写清楚** —— ESP32-P4 TRM（MIPI-DSI bridge、PSRAM controller、AXI interconnect）+ `esp_lcd_panel_dpi.c` 的 DMA link-list。社区目前对"这颗 SoC 能承受几个并发的 PSRAM 写入者"没有明确答案，而本项目有一个大致可复现的样本。
- [ ] **之后**再考虑提高帧率上限：放在开关后面，带上数据。

## 2. 把这个版本没用的硬件用起来

- [ ] **音频频谱（ES8388 + ES7210）。** StickS3 和 Cardputer 版本有 40 段彩条可视化，Tab5 这边一根线都没接。需要 P4 上的 I²S + codec 初始化。一个有意思的设计约束：它不能变成常驻的屏幕杂物（这一系列时钟把仪表当**事件**，不当家具）。
- [ ] **BMI270 摇一摇 → 换字符集。** 和 StickS3 同一颗芯片，那边 `updateIMU()`/`checkShake()` 已经能用，移植是小事。也可以顺手做成"拿起时钟"唤醒。
- [ ] **INA226 电量计。** 读出来，然后诚实地决定它放哪：本项目的规矩是功能性信息**事件化**，不做常驻 HUD——淡入一次低电量提醒，比常驻一个百分比更合适。
- [ ] **RX8130CE 闹钟中断** → 整分/整秒唤醒（和 §3 很配）。

## 3. 省电

- [ ] **Light Sleep** + GPIO（IMU 中断）/ RTC 唤醒。现在的"闲置"只是调暗、降到 8fps、关背光——CPU 从未睡眠，所以闲置电流离硬件潜力差得远。
- [ ] **用 INA226 实测并公布各档闲置电流**。要数字，不要形容词。

## 4. 配网与交互

- [ ] **非 ASCII 的 SSID。** 现在中文 SSID 显示成 `□`。两件事：(a) 一个小型 CJK 位图字库（技术和片假名集一样，`tools/fontgen.py` 已经在生成 16×16 1bpp 字形）；(b) 屏幕键盘支持非 ASCII **密码**输入。
- [ ] **带夏令时规则的具名时区**，取代裸的 UTC 偏移网格。存偏移 + DST 规则（或者直接存 TZ 字符串交给 SNTP）。
- [ ] **手动对时**作为兜底：现在首次开机如果没有可用热点，就完全没有钟。
- [ ] **滑动手势**作为快捷层：竖向滑动 = 调亮度，这样不必进设置菜单。
- [ ] 可选：**秒针/日期/秒数样式的变体**——可以做成选项，不要常驻添加。

## 5. 字库与许可卫生

- [ ] **用 OFL 字体重新生成 `katakana_font.h`**（Noto Sans JP、M PLUS 等），让下游再分发没有歧义——见 [CREDITS.md](CREDITS.md)。字形会有细微变化，需要并排渲染对比；作者对 16×16 下片假名的观感很挑。
- [ ] **给 `tools/fontgen.py` 加 `--font` 参数**，默认给一个文档化的 OFL 字体；它现在硬编码了一个 macOS 系统字体路径。

## 6. 构建、打包与 CI

- [ ] **GitHub Actions 构建检查** —— 用固定工具链跑 pioarduino 平台，证明每个 PR 都能在干净树里编过，并把合并镜像作为 artifact 附上。
- [ ] **`src/override_toolchain.py` 自动探测 RISC-V 工具链**，取代硬编码的 `esp-14.2.0_20260121` 路径（这是新人最容易撞的构建失败）。
- [ ] **`tools/snap.py`** —— 串口截图辅助脚本，处理好固件那段 921600 波特率切换并写出 BMP；然后接进文档，让贡献者能给 PR 附截图。
- [ ] **打 tag 的发布流程**：附上合并 `.bin`、M5Burner `.zip` 和 `sha256sums.txt`。

## 7. 文档

- [ ] Windows 与 Linux 的构建走查（现在这版是 macOS 味儿的）。
- [x] README 里放截图——已加入第一张真机实拍。还缺一段**屏幕在动**的短视频：没有什么比"看见它在跑"更能说明问题。
- [ ] 架构笔记：发光缓冲/PAL 的设计、绘制预算、为什么热路径里没有 sprite。
- [ ] 更多语言的翻译。

## 明确不做

- **任何《骇客帝国》的素材。** 不用电影剧照、logo 或"数字雨"商标图形——这保持为一个无关联的同人项目，不含华纳兄弟的任何素材。
- **"应该没事"就再加一个帧缓冲写入者。** 更深的队列 + 三缓冲在别的板子上是标准答案，在这块板子上是错的（见移植笔记）；类似做法必须先有实测 + 运行时开关。
- **引入调试控制台工作流。** 用眼睛验证是本项目的约定；而在这颗 SoC 上，话多的串口会话是显示通路的加重因素。
